apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: beautyrec-model-cache
  namespace: beautyrec
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
